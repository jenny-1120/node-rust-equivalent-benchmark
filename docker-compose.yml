name: node-rust-equivalent-benchmark

services:
  node-api:
    build:
      context: ./services/node-api
    container_name: node-rust-equivalent-node-api
    cpus: 2.0
    mem_limit: 2g
    env_file:
      - ./services/common.env
      - ./services/node-api/.env
    volumes:
      - ./data:/app/data:ro
    ports:
      - "3101:3001"

  rust-api:
    build:
      context: ./services/rust-api
    container_name: node-rust-equivalent-rust-api
    cpus: 2.0
    mem_limit: 2g
    env_file:
      - ./services/common.env
      - ./services/rust-api/.env
    volumes:
      - ./data:/app/data:ro
    ports:
      - "3102:3002"

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    container_name: node-rust-equivalent-cadvisor
    privileged: true
    devices:
      - /dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk:/dev/disk:ro
    ports:
      - "38080:8080"
    command:
      - "--docker_only=true"
      - "--store_container_labels=false"
      - "--disable_metrics=percpu,sched,tcp,udp,disk,diskIO,hugetlb,referenced_memory,cpu_topology,resctrl"

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: node-rust-equivalent-prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    volumes:
      - ./observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "39090:9090"
    depends_on:
      - node-api
      - rust-api
      - cadvisor

  grafana:
    image: grafana/grafana:11.1.4
    container_name: node-rust-equivalent-grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3300:3000"
    depends_on:
      - prometheus

  k6-bench:
    image: grafana/k6:0.53.0
    container_name: node-rust-equivalent-k6-bench
    user: "0:0"
    cpus: 1.0
    mem_limit: 1g
    volumes:
      - ./bench/k6:/scripts:ro
      - ./results:/results
    depends_on:
      - node-api
      - rust-api
